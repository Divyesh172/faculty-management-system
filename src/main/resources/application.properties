server.port=${PORT:8080}

spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# Application Name
spring.application.name=Faculty Management System

# Database Connection
# LOGIC: Use 'DB_HOST' env variable if valid, otherwise default to 'localhost'
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/faculty_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Divyesh@2025!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect